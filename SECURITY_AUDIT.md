# 보안 감사 — 2026-09-10

범위: Express API, Google OAuth·세션, 공개 조회, 입력과 SQLite 저장, Web Push, Docker·백업 설정. 로컬 임시 DB와 가짜 OAuth 클라이언트 값으로 제한된 요청만 실행했다. 실제 Google 계정, 운영 도메인 또는 사용자 서버에 침투·부하 테스트를 하지 않았다.

결과: 기존 코드에서 **중간 위험 2건, 낮은 위험 2건**을 재현한 뒤, 사용자 요청에 따라 4건 모두 수정했다. 아래 재현 설명은 수정 전 증거이며, 각 상태에 수정 내용과 로컬 재검사 결과를 기록했다. 운영 배포와 실제 Google 로그인은 별도 검증이 필요하다.

## 확인된 이슈

### AUD-01 — 중간: 인증 없는 OAuth 시작 요청의 제한 부재

- 위치: `src/server/auth.ts`의 `GET /auth/google`, `src/server/store.ts`의 oauth 정리.
- 조건: Google 클라이언트 설정이 활성화되고 로그인 시작 경로를 외부에서 호출할 수 있는 운영 환경.
- 재현: 인증 쿠키 없이 8회 요청했을 때 모두 302이며 oauth 테이블에 8개의 행이 생성됨. 실제 Google 서비스에 토큰 교환 요청은 보내지 않음.
- 영향: 공격자가 매 요청마다 SQLite 쓰기와 state 행을 생성할 수 있음. 만료 10분과 주기적 정리는 있지만 생성 속도·동시 미완료 요청 상한이 없어 자원 소모를 제한하지 못함. 대량 부하로 서비스 중단까지 입증한 것은 아님.
- 권고: 로그인 시작/콜백에 요청 제한과 미완료 state 총량 제한을 적용하고, 실제 Caddy 구성에 맞는 프록시 신뢰 정책을 명시. 보호되지 않은 공개 API의 반복 조회도 별도 제한 검토.
- 상태: 수정. 단일 소유자 앱 기준 프로세스 전체 로그인 시작 5회/분, 콜백 20회/분 제한과 Retry-After를 추가했다. 미완료 state는 SQLite 트랜잭션 안에서 만료 정리 후 최대 100개로 제한한다. 8회 요청 중 5회만 302, 나머지 429, 저장 행 5개를 확인했다. 전달된 IP 헤더를 신뢰하지 않으므로 위조로 제한을 우회할 수 없다. 메모리 속도 제한은 재시작 시 초기화되지만 DB 총량 상한은 유지된다. 전역 제한이므로 타인의 요청으로 정상 로그인도 일시 제한될 수 있으며, 실제 Caddy의 경계 방어는 별도 설정해야 한다.

### AUD-04 — 중간·조건부: 백업이 0644 파일·0755 디렉터리로 생성됨

- 위치: `scripts/backup.mjs`의 mkdir 및 backup 호출.
- 재현: 기본 umask 022에서 실행한 백업 파일 권한은 0644, 생성 디렉터리는 0755.
- 영향: 상위 경로를 다른 로컬 계정이 탐색할 수 있는 배포에서 비공개 일정·메모와 DB 내 민감 데이터가 들어 있는 백업을 읽을 수 있음. 검증은 임시 보호 디렉터리 안에서 수행했고 실제 데이터 노출은 발생하지 않음. Docker 전용 볼륨만 격리해 사용하는 경우 다른 계정의 경로 접근 조건을 따로 확인해야 함.
- 권고: 백업 프로세스 umask 077, 새 백업 디렉터리 0700, 파일 0600을 명시하고 기존 파일도 검사. 서버 밖 보관 시 접근 권한과 암호화 정책 적용.
- 상태: 수정. 백업 프로세스 umask 077, 새 디렉터리 0700, 독점 생성 파일 0600을 적용했다. UUID 파일명으로 동시 실행의 덮어쓰기를 방지한다. 기존 대상 디렉터리가 다른 사용자에게 열려 있거나 심볼릭 링크이면 쓰기 전에 중단한다. 기존 경로나 백업 파일 권한은 임의 변경하지 않는다. 기존 백업이 있는 운영 환경에서는 해당 파일의 소유권·권한을 확인하고 전용 디렉터리를 0700으로 준비해야 한다. umask 022 재검사에서 파일 0600·디렉터리 0700을 확인했다.

### AUD-02 — 낮음: 비ASCII OAuth state가 400 대신 500을 발생시킴

- 위치: `src/server/auth.ts` 콜백의 `state.length` 비교와 `timingSafeEqual`.
- 재현: ASCII 43자 쿠키와 `é` 43자인 state는 JavaScript 문자 길이가 같지만 UTF-8 바이트 길이가 다름. `timingSafeEqual` 예외가 발생하여 HTTP 500을 반환.
- 영향: 인증 우회는 확인되지 않음. 잘못된 입력으로 내부 오류·로그를 유발하며 모니터링을 오염시킬 수 있음.
- 권고: state·쿠키가 발급 형식인 base64url 43자인지 먼저 검증하고 바이트 길이를 비교. 잘못된 입력은 일관되게 400으로 종료.
- 상태: 수정. state와 쿠키 모두 base64url 43자 형식을 먼저 검사한 뒤 timingSafeEqual을 실행한다. 비ASCII·잘못된 길이·형식·불일치 입력이 400으로 종료되는 것을 확인했다.

### AUD-03 — 낮음: 로그아웃 후 SSE에 변경 신호가 잠시 전달됨

- 위치: `src/server/app.ts`의 clients/broadcast와 20초 heartbeat, `src/server/auth.ts`의 logout.
- 재현: 세션으로 `/api/events` 연결 → 해당 세션 로그아웃 → 별도 정상 소유자 세션에서 일정 생성. 기존 스트림에서 `event: change`와 빈 `{}` 수신.
- 영향: 정상 데이터 API는 로그아웃 후 차단되지만 기존 SSE에는 다음 권한 확인까지 활동 시각이 전달될 수 있음. 일정 제목이나 본문 데이터는 이 이벤트에 포함되지 않음.
- 권고: 스트림을 세션에 연결해 로그아웃 시 즉시 닫거나 각 broadcast 직전에 세션 유효성을 검증.
- 상태: 수정. SSE 연결을 세션 해시에 연결하고 로그아웃 시 해당 세션의 연결을 즉시 닫는다. 매 broadcast 직전에도 세션 유효성을 확인한다. 로그아웃된 스트림 종료, 다른 세션의 정상 수신, 만료된 세션의 변경 신호 차단을 확인했다.

## 검증된 방어와 한계

- 미인증 일정 조회·생성·변경 및 키 관리가 차단됨. 원문 공유 키를 가진 방문자도 공개된 항목만 조회하며, 에이전트 키는 별도 권한 체계로 제한됨.
- 브라우저 변경 요청은 같은 Origin과 JSON을 요구함. 에이전트 라우터만 유효한 Bearer 인증으로 Origin 없는 서버 요청을 허용하며 쿠키로 대체 인증하지 않음.
- SQL 매개변수를 바인딩하고, 일정 입력은 서버에서 정규화함. 공개 응답은 전용 필드 목록이며 원래 시리즈/회차 식별자를 그대로 노출하지 않음.
- React 텍스트 렌더링, CSP와 보안 헤더, HttpOnly/Secure/SameSite 쿠키 설정을 확인. 실브라우저 모든 XSS 페이로드나 배포된 Caddy 헤더를 포괄적으로 검사한 것은 아님.
- Google의 인증코드 흐름·PKCE·state·nonce 및 라이브러리 토큰 검증을 사용. 실제 허용/비허용 Google 계정 로그인 성공 흐름은 인증 설정 후 별도 검증이 필요함.
- 푸시 URL은 HTTPS와 고정된 서비스 호스트로 제한. 자격 증명 포함 URL·비표준 포트·임의 호스트 거부를 확인. 라이브러리의 실제 푸시 서비스 전송은 이번 로컬 감사에서 수행하지 않음.
- Docker는 비루트, 읽기 전용 루트 파일시스템, 별도 데이터 볼륨, 권한 제거 설정. 실제 서버의 네트워크 노출·Caddy 로그·호스트 권한·백업 반출 정책은 미검증.
- `npm audit --omit=dev --json`: 실행 시점에 보고된 런타임 의존성 취약점 **0건**. 이것은 소스나 배포가 취약점 없이 안전하다는 보증이 아님.

## 신규 에이전트 API의 보안 설계

- 원문 키 일회성 반환, 256비트 난수와 해시 저장. Google 소유자 계정에 연결, 1~365일 만료·즉시 폐기.
- 읽기·쓰기·삭제 권한 분리. 키 관리·공유 링크·브라우저 세션·푸시 설정에 에이전트 키를 사용할 수 없음.
- 에이전트 경로에서 소유자 쿠키와 로컬 데모 자동 인증을 사용하지 않음.
- 키당 120회/분, 잘못된 인증은 연결 IP당 30회/분. 메모리 버킷 수 제한. 재시작 시 버킷은 초기화되므로 분산·영구 방어를 보장하지 않음.
- 변경 요청에는 멱등성 키가 필요하며 권한 확인 후에만 재시도 결과를 반환. 원래 변경과 재시도 기록을 동일 트랜잭션에 저장.
- 내용·키·원본 URL을 제외한 요청 감사 기록. 재시도 기록에는 원래 성공 응답의 일정 내용이 포함될 수 있으므로 DB/백업 보호가 계속 필요함.
- 기존 브라우저 기능 25개와 신규 API 테스트 10개, 합계 **35개 테스트 통과**. 키 권한·만료/폐기·계정 변경·Origin·관리 API 격리·멱등성·버전 충돌·삭제/복구·페이지 조회·요청 제한·롤백을 포함.

## 재현과 근거

```sh
npm run check
npm test
node scripts/security-audit.mjs
npm audit --omit=dev --json
```

수정 후 정량 결과는 `artifacts/security-audit.json`, 재현 코드는 `scripts/security-audit.mjs`에 있다. 재현 스크립트는 4건의 방어 결과가 기대값과 다르면 실패한다. `tests/security.test.ts`에 요청 제한·미완료 state 상한·입력 형식·SSE 세션 격리·백업 권한의 회귀 검사 5개를 추가했다.

수정 후 `npm run check`, 전체 **40개 테스트**, 4건 재현 회귀 스크립트가 모두 통과했다. UI·운영 컨테이너·Caddy 설정은 변경하지 않았다.

참고 기준: [OWASP REST Security](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html), [Google OpenID Connect](https://developers.google.com/identity/openid-connect/openid-connect).
