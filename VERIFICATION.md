# 초기 구현 검증 기록

검증일: 2026-09-10

## 통과한 검사

- TypeScript: `npm run check`.
- 자동 테스트: `npm test`, 25개. 날짜·시간 변환, 윤년·말일·마지막 요일·격주·반복 종료, 회차 이동·완료·공개 예외, 범위별 수정·삭제·복구, 전체 편집 시 완료 전파 방지, 조회 권한·Origin 차단, 공유 링크 만료, 세션·OAuth 실패 처리, SQLite 재연결·백업 복구, 푸시 발송 기록.
- Chromium 브라우저: 1440px PC 및 390px 모바일 화면. 브라우저 시간대를 미국 로스앤젤레스로 두고도 한국 시간으로 표시되는지 확인.
- 브라우저 흐름: 생성 → 완료 → 삭제 → 휴지통 복구, 달력 드래그 이동과 기간 조절, 두 창 사이 자동 동기화, 월·주·일 전환, 겹치는 일정의 나란한 배치, 공개 정보 필터링과 링크 비활성화, 매월 마지막 월요일 입력.
- 브라우저 실행 오류 0건, 모바일 가로 넘침 없음.
- Docker: `plan-app:local` 이미지 빌드 성공. 빌드 내부 타입 검사 및 자동 테스트 통과.
- Docker 실행: 비루트 사용자·읽기 전용 루트 파일시스템·별도 SQLite 볼륨으로 실행. `/healthz` 정상, 인증 설정이 없는 운영 모드에서는 소유자 권한 비활성.
- Docker 데이터: 검증용 값을 SQLite에 저장하고 컨테이너 재시작 후 유지 확인. 온라인 백업 생성 및 `integrity_check` 통과.

## 화면

- [PC 월간 달력](artifacts/desktop-month.png)
- [PC 주간 시간표](artifacts/desktop-week.png)
- [휴대폰 월간 달력과 목록](artifacts/mobile-month.png)
- [휴대폰 일정 편집](artifacts/mobile-editor.png)
- [읽기 전용 공유 화면](artifacts/shared.png)

화면과 기능 검증에는 별도 임시 데모 DB를 사용했다. 테스트 도구는 기존 Playwright 설치를 사용했으며 앱 패키지에 브라우저 도구를 추가하지 않았다.

## 외부 설정 후 확인할 사항

- 실제 Google OAuth 성공 로그인과 허용 계정 확인. 현재 자동 검증은 미인증 접근·세션 만료·위조 상태 차단과 권한 적용을 대상으로 한다.
- VAPID 키 등록 후 실제 PC·안드로이드 알림 허용, 앱을 닫은 상태의 수신. 자동 테스트에서는 전송 함수를 대체하여 예약·중복 억제·완료 취소 로직을 검증한다.
- 기존 Caddy 네트워크와 `plan.crasnec.com`의 HTTPS 연결. 서비스 배포는 아직 수행하지 않았다.
- 실제 운영 서버의 백업 외부 보관 주기와 복구 절차.
