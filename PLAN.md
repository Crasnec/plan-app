# Plan — 개인 일정 및 할 일 앱

작성일: 2026-09-10
상태: 초기 구현 완료 및 검증 진행. 실제 OAuth·푸시 키와 Caddy 연결 정보는 사용자 제공 후 연결한다. 아래 원래 계획과 달라진 구현 결정은 12절에 기록한다.
서비스 주소: https://plan.crasnec.com

## 1. 제품 방향

개인 할 일과 일정을 하나의 목록에서 관리하고, PC와 안드로이드 휴대폰에서 동기화한다. 소유자는 편집할 수 있고, 방문자는 공유 링크를 통해 공개한 항목만 볼 수 있다.

디자인은 선택한 크림·세이지 시안을 바탕으로 한다. 따뜻한 크림 배경, 차분한 녹색 강조색, 둥근 카드와 충분한 여백을 사용한다. 시안의 분위기를 유지하되 PC와 모바일에 맞게 화면을 구성한다.

## 2. 확정 요구사항

### 계정과 공유

- Google 로그인 사용. 편집 권한은 검증된 이메일이 `crasnec@gmail.com`인 계정 하나에만 부여한다.
- 허용 이메일은 서버 환경변수 `OWNER_EMAIL`로 관리한다.
- 소유자만 추가·수정·삭제·완료 처리와 공개 설정을 할 수 있다.
- 로그인 없는 읽기 전용 공유 링크 하나에서 공개된 항목 전체를 조회한다.
- 항목은 기본 비공개이며, 공개한 항목의 제목·메모·일정·완료 상태를 표시한다.
- 방문자는 완료 항목을 숨길 수 있다.
- 반복 일정의 공개 여부는 전체 설정을 따르되, 특정 회차만 변경할 수 있다.

### 할 일과 일정

- 목록은 하나다.
- 제목, 메모, 시작일, 종료일, 선택적인 시작 시간과 종료 시간, 완료 여부를 관리한다.
- 날짜가 없는 항목은 ‘날짜 미정’으로 모은다.
- 날짜만 있으면 ‘종일’ 일정이다.
- 시간이 있는 일정과 여러 날에 걸친 일정을 지원한다.
- 일정 겹침은 정상적인 상태다. 경고나 오류를 표시하지 않고 시간표에서 나란히 배치한다.
- 삭제 항목은 휴지통에서 30일간 복구 가능하다.

### 달력과 조작

- 월요일부터 한 주를 시작한다.
- 기본은 월간 보기이며 주간·일간 시간표도 제공한다.
- PC: 달력과 선택한 날짜의 목록을 나란히 표시하고, 드래그로 일정 이동과 기간 조절을 지원한다.
- 휴대폰: 달력 아래에 선택한 날짜의 목록을 표시하고, 항목을 눌러 편집한다.
- 조회·편집은 온라인 사용을 전제로 한다. 오프라인 편집은 초기 범위에서 제외한다.

### 반복

- 매일, 매주 지정 요일, 격주를 비롯한 반복 간격을 지원한다.
- 월별 지정 날짜, 매월 둘째 월요일 같은 순서 기반 요일, 매월 말일, 매월 마지막 월요일 같은 마지막 요일을 지원한다.
- ‘말일’과 ‘마지막 요일’은 별도 명시적 옵션이다. ‘31일 반복을 말일로 보정’하는 규칙은 채택하지 않는다.
- 반복 종료일은 선택 사항이다.
- 수정·삭제 범위는 ‘이번 일정만’, ‘이번부터 이후 일정’, ‘전체 일정’ 중 선택한다.
- 완료 여부는 회차별로 관리한다.

### 시간과 알림

- 시간이 있는 일정의 시각은 UTC로 저장한다.
- 입력과 표시는 한국 시간 `Asia/Seoul`로 고정한다.
- 종일 일정은 UTC 시각으로 변환하지 않고 날짜로 저장한다.
- 반복은 한국 시간 기준으로 계산한다.
- 알림은 소유자에게만 보내며 PC와 안드로이드에서 앱을 닫은 상태에서도 수신하는 것을 목표로 한다.
- 시간 지정 일정의 기본 알림은 시작 10분 전이다.
- 종일 일정의 기본 알림은 시작일 오전 9시 한국 시간이다.
- 날짜 미정 항목에는 알림이 없다.
- 일정별로 알림 시간을 변경하거나 끌 수 있다.

### 기술과 배포

- 프런트엔드: TypeScript + React.
- 백엔드: Node.js + TypeScript.
- 초기 데이터베이스: SQLite.
- Docker로 배포하며 SQLite는 영구 볼륨에 저장한다.
- 기존 Caddy는 컨테이너로 실행 중이다. 앱을 Caddy와 같은 Docker 네트워크에 연결한다.
- Caddy가 `plan.crasnec.com`의 HTTPS와 리버스 프록시를 처리한다.

## 3. 화면 구성 제안

### 소유자 화면

- 상단: 오늘로 이동, 이전·다음 기간, 현재 기간, 월·주·일 전환, 새 할 일.
- 본문: 달력과 선택 날짜 목록. 월간 셀에는 일정 요약과 넘치는 항목 수를 표시한다.
- 목록: 시간순 일정, 종일 일정, 날짜 미정 접근, 전체·진행 중·완료 필터.
- 편집 화면: 제목, 메모, 날짜 여부, 종일 여부, 시작·종료, 반복, 알림, 공개 여부.
- 반복 수정·삭제 시 적용 범위 선택.
- 관리 화면: 공유 링크, 휴지통, 기기 알림 설정, 로그아웃.
- PC에서는 편집 패널, 모바일에서는 전체 화면 또는 충분히 큰 편집 시트를 사용한다.

### 공유 화면

- 동일한 디자인과 달력 보기, 공개 항목의 날짜 미정 목록, 완료 항목 숨기기 기능을 제공한다.
- 편집 기능은 제공하지 않는다. 서버가 조회 권한과 공개 범위를 검사한다.
- 비공개 항목은 응답 데이터, 항목 수, 달력 표시, 상세 화면 어디에도 포함하지 않는다.

## 4. 기술 구조 제안

초기에는 하나의 저장소에서 React 앱과 Node.js 서버를 함께 관리한다. 서버가 빌드된 프런트엔드와 API를 같은 출처에서 제공하면 인증 쿠키와 배포 구성이 단순해진다.

제안 디렉터리:

```text
plan-app/
  PLAN.md
  apps/web/          React 화면
  apps/server/       API, 인증, 반복 계산, 알림 작업
  packages/domain/   공통 타입과 날짜·반복 도메인 로직
  migrations/       SQLite 마이그레이션
  deploy/           Docker 및 Caddy 설정 예시
```

- 서버 프레임워크, 데이터 접근 계층, 달력·반복 라이브러리는 구현 전에 유지보수 상태와 라이선스, React 연동, 필요한 반복 규칙 지원을 확인한 뒤 선택한다.
- API 요청·응답을 검증하고 날짜 및 반복 계산은 서버를 기준으로 한다.
- 초기에는 SQLite를 사용하는 앱 인스턴스 하나와 알림 작업 실행부로 구성한다. 알림 작업은 같은 서버 프로세스에서 시작할 수 있다.
- 기기 간 변경 반영은 서버 이벤트와 재조회 방식으로 제안한다. 재연결 및 창 활성화 때도 최신 데이터를 조회한다.
- 동시에 편집한 경우 버전 필드로 충돌을 감지하고 사용자에게 최신 내용을 보여준다. 일정 시간 겹침과 데이터 편집 충돌은 별개다.

## 5. 데이터 모델 제안

| 데이터 | 역할 |
| --- | --- |
| owner_identity | Google의 검증된 계정 식별자 및 허용된 소유자 |
| sessions | 만료·폐기가 가능한 로그인 세션 |
| items | 단일 일정 또는 반복 시리즈의 제목, 메모, 일정 종류, 공개 설정, 알림 설정, 버전 |
| recurrence_rules | 간격, 요일·월 날짜·순서·말일 조건, 한국 시간 기준점, 종료일 |
| occurrence_overrides | 원래 회차 식별자 기준의 개별 수정, 완료, 공개 설정, 삭제 예외 |
| share_link | 공유 토큰의 해시, 활성 여부, 발급 정보 |
| push_subscriptions | 소유자 기기별 푸시 구독 |
| notification_jobs | 회차·기기별 발송 예정 시각, 재시도 및 처리 상태 |
| trash_entries | 삭제 범위, 복원 정보, 삭제 시각, 영구 삭제 예정 시각 |

일정 종류는 `undated`, `all_day`, `timed`로 구분한다. 시간 지정 일정은 UTC 시작·종료를 저장하고, 종일 일정은 시작·종료 날짜를 저장한다. 종일 종료 날짜의 내부 포함 여부는 하나로 통일하며, 제안은 내부 종료일을 제외 경계로 저장하고 사용자에게는 마지막 포함 날짜를 표시하는 방식이다.

반복 시리즈는 한국 시간의 시작 기준과 규칙을 보관한다. 미래 회차를 무한히 저장하지 않고 조회 기간과 알림 준비 기간에 필요한 회차만 계산한다. 계산된 시간 지정 회차와 알림 시각은 UTC로 처리한다.

## 6. 반복과 삭제 처리 제안

- 회차는 변경 후 날짜가 아니라 원래 반복 규칙에서 생성된 회차 기준으로 식별한다. 날짜를 옮겨도 완료·공개 설정의 연결이 유지되어야 한다.
- ‘이번만’은 회차 예외로 저장한다.
- ‘이번부터 이후’는 시리즈를 분리하고 기존 예외의 이전·보존 정책을 적용한다.
- ‘전체’ 변경 시 기존 완료 및 예외 처리 규칙을 명확히 정한 뒤 구현한다.
- 회차 이동으로 원래 조회 기간 밖에서 안으로 들어온 일정도 조회에 포함한다.
- 반복 삭제도 선택한 범위를 복구할 수 있어야 한다. 30일 후 관련 복원 데이터를 정리한다.
- 일정 변경·삭제·복구·완료 시 관련 알림 예약을 함께 갱신한다.
- 구체적인 말일·마지막 요일 계산과 월 경계, 윤년, 여러 날 일정의 반복은 도메인 테스트로 검증한다.

## 7. 인증과 공유 제안

- 서버에서 Google 인증 응답의 유효성과 검증된 이메일을 확인한다. 이메일이 허용 목록과 일치한 뒤 안정적인 Google 계정 식별자로 세션을 연결한다.
- 다른 Google 계정으로 로그인해도 소유자 권한을 부여하지 않는다.
- 세션 쿠키에는 HttpOnly, Secure와 적절한 SameSite 정책을 적용하고 변경 요청을 보호한다.
- 공유 링크는 충분히 긴 무작위 토큰을 사용하고 서버에는 해시를 저장한다.
- 공유 링크 재발급·비활성화 기능을 제안한다. 재발급 시 기존 링크는 즉시 무효화한다.
- 공유 응답은 전용 공개 데이터 형태로 구성하고 검색 엔진 색인을 허용하지 않는다.
- 공유 링크를 아는 사람은 열람 가능하다는 점을 공유 설정에서 설명한다.

## 8. 알림 처리 제안

- 브라우저 Push API, 서비스 워커, 서버 Web Push를 사용하는 구성을 검토한다.
- 로그인한 소유자가 기기별로 알림을 허용하고 구독을 등록한다. 방문자는 구독할 수 없다.
- 서버가 가까운 미래의 알림 작업을 준비하고 재시작 후에도 미처리 작업을 복구한다.
- 회차·기기·알림 기준으로 중복 작업을 방지하고 일시 실패는 제한적으로 재시도한다.
- 만료된 구독은 정리한다. 수정·삭제된 일정의 과거 예약이 발송되지 않도록 발송 직전 최신 상태를 확인한다.
- 푸시 전송 이후 표시 여부는 브라우저, 운영체제, 네트워크 및 배터리 설정에 영향을 받는다. 앱을 닫은 상태의 동작은 실제 사용하는 PC 브라우저와 안드로이드 기기에서 검증한다.
- 완료된 회차의 예약 취소와 서버 중단 후 지연 알림 허용 범위는 구현 전에 합의한다.

## 9. 배포와 운영 제안

- Docker Compose로 앱을 실행하고 기존 Caddy의 외부 Docker 네트워크에 참여시킨다.
- Caddy는 앱의 내부 포트로 프록시한다. 실제 네트워크 이름과 Caddy 설정 위치는 서버 점검 후 확정한다.
- SQLite 파일과 관련 저널 파일은 동일한 영구 볼륨에 둔다. 초기에는 단일 서버·단일 앱 인스턴스로 운영한다.
- 마이그레이션은 배포 시 명시적으로 실행하며, 파괴적인 변경 전에는 백업한다.
- SQLite의 일관된 온라인 백업 방식을 사용하고 실제 복구 절차도 검증한다. 실행 중인 DB 파일만 단순 복사하는 방식은 피한다.
- 헬스 체크, 프로세스 재시작, 로그와 저장 공간 관찰, 휴지통 정리 작업을 마련한다.
- 인증 토큰, 공유 토큰, 푸시 구독 비밀값은 로그에 남기지 않는다.
- 환경변수 예시: `APP_ORIGIN`, `OWNER_EMAIL`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET`, `DATABASE_PATH`, `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`.
- Google OAuth의 리디렉션 URL과 푸시 설정은 실제 인증 경로 결정 후 등록한다. 비밀값은 저장소에 커밋하지 않는다.

## 10. 구현 순서와 완료 기준

1. 화면 흐름과 남은 규칙 확정: PC·모바일 달력, 편집 폼, 반복 설정, 공유 화면을 검토한다.
2. 프로젝트 기반: React·Node.js·TypeScript, SQLite 마이그레이션, Docker 개발 및 배포 구성을 준비한다.
3. 소유자 로그인과 기본 일정: 인증, 날짜 미정·종일·시간 지정·여러 날 일정, 완료와 휴지통을 구현한다.
4. 달력과 동기화: 월·주·일 보기, 월요일 시작, 겹침 배치, PC 드래그, 모바일 편집과 기기 간 반영을 구현한다.
5. 반복: 반복 규칙, 회차 예외, 범위별 변경·삭제·복구, 회차별 완료 및 공개를 구현한다.
6. 공유: 공개 전용 조회, 완료 필터, 링크 관리와 권한 검증을 구현한다.
7. 알림: 구독, 예약, 변경 반영, 재시도와 실제 기기 수신을 구현한다.
8. 배포: Caddy 연결, HTTPS, 인증 설정, 백업·복원 및 실제 사용 시나리오를 검증한다.

주요 검증 항목:

- 소유자 외에는 어떤 변경 API도 실행할 수 없고, 공유 API에는 비공개 항목이나 메타데이터가 노출되지 않는다.
- UTC 저장과 한국 시간 표시가 일치하며 종일 날짜가 하루씩 이동하지 않는다.
- 월·주·일 보기 모두 월요일 시작과 기간 경계를 올바르게 처리한다.
- 겹치는 일정은 저장 가능하고 경고 없이 읽기 좋게 배치된다.
- 말일·마지막 요일·격주·종료일·여러 날 반복과 회차별 예외가 올바르게 동작한다.
- 반복 범위별 수정·삭제 및 휴지통 복구가 기존 회차 상태를 의도대로 보존한다.
- PC 변경이 휴대폰에 반영되고 동시 수정 시 조용히 데이터를 덮어쓰지 않는다.
- 일정 변경 후 취소된 알림이 발송되지 않으며 서버 재시작으로 예약이 사라지지 않는다.
- 실제 안드로이드와 PC에서 앱을 닫은 상태의 알림을 검증한다.
- 컨테이너 재생성 후 데이터가 유지되고 백업에서 복원 가능하다.

## 11. 구현 전 남은 결정

아래는 아직 사용자 확정 사항이 아니다. 화면 및 도메인 설계 검토에서 순서대로 결정한다.

- 고정된 월 날짜가 없는 달의 처리와 다섯째 요일이 없는 달의 처리. ‘말일’ 및 ‘마지막 요일’ 옵션과 구분한다.
- 반복 전체 변경이나 시리즈 분리 때 기존 회차 예외·완료 기록을 어떻게 보존할지.
- 여러 날 종일 일정의 반복 길이, 종료일의 포함 규칙과 겹치는 회차 표시.
- 완료한 회차의 알림 취소, 지연 알림 허용 시간, 종일 알림의 변경 가능한 범위.
- 삭제·복구와 동시에 발생하는 수정 및 반복 알림 예약의 처리.
- 사용할 서버·DB·달력·반복 라이브러리와 필요한 라이선스.
- 실제 서버 접근 경로, Caddy 네트워크 이름, 배포 디렉터리 및 백업 보관 정책.

## 12. 구현 결정 및 현재 상태

- 사용자의 구현 지시에 따라 React·TypeScript, Express, Node.js 내장 SQLite를 사용했다. 달력·반복·시간표·겹침 배치·드래그는 직접 구현하고 Google 인증과 Web Push의 표준 처리는 전용 라이브러리를 사용한다.
- 소스는 `src/web`, `src/server`, `src/shared`, 검증은 `tests`, 배포 예시는 `deploy`에 둔다. 단일 프로젝트 구성으로 초기 개발과 배포를 단순화했다.
- 날짜가 없는 월 31일이나 다섯째 요일은 건너뛴다. 말일과 마지막 요일은 명시적 별도 규칙이다.
- 반복 변경에서 기존 회차의 명시적 예외를 보존한다. 새 규칙에서 사라진 예외 회차는 독립 일정으로 남긴다. 완료한 회차의 상태가 전체 또는 미래 회차로 확산되지 않게 처리한다.
- 알림은 현재 일정에서 발송 대상을 매 30초 계산하고 SQLite에 기기·회차·예정 시각별 발송 기록을 저장한다. 완료된 회차는 취소하고 재시작 후 15분 이내 지연 알림만 처리한다.
- 초기 스키마는 SQLite 버전 테이블과 함께 생성한다. 외부 마이그레이션 도구나 ORM은 사용하지 않는다.
- 인증 없는 로컬 미리보기는 별도 데모 DB와 localhost 바인딩을 사용하며 운영 환경에서는 실행을 거부한다.
- Docker 이미지 빌드와 테스트 컨테이너 검증은 실제 서비스 배포와 별개로 수행한다. Google 로그인 성공 흐름, 실제 푸시 수신 및 기존 Caddy 연결은 설정 제공 후 검증한다.
- 실행과 배포, 백업·복구 및 구체적인 예외 정책은 `README.md`에 정리한다.
